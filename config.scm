;; Config file for minigit

;; Style sheet for the web interface
(define style "style.css")

;; The folder to use as a repository list
(define folder "../")

;; The port to expose the interface on
(define port 8080)
